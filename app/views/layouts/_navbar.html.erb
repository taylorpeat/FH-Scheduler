<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">FH Scheduler</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active"><a href=<%= rosters_path %>>Index<span class="sr-only">(current)</span></a></li>
        <li><a href=<%= team_schedule_path %>>NHL Team Schedule</a></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Rosters <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% if current_user %>
              <% current_user.rosters.each do |roster| %>
                <li><a href=<%= roster_path(roster) %>><%= roster.name %></a></li>
              <% end %>
              <li role="separator" class="divider"></li>
              <li><a href=<%= new_roster_path %>><i class="glyphicon glyphicon-plus"></i> New Roster</a></li>
            <% end %>
          </ul>
        </li>
      </ul>
      <% if current_user %>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="#">Log Out</a></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.username || "Guest" %> <span class="caret"></span></a>
          <% if current_user && current_user.username %>
            <ul class="dropdown-menu">
              <li><a href='#'>Edit User</a></li>
              <li><a href=<%= logout_path %>>Log Out</a></li>
            </ul>
          <% else %>
            <ul class="dropdown-menu">
              <li><a href=<%= register_path %>>Register New User</a></li>
              <li><a href=<%= logout_path %>>Log Out</a></li>
            </ul>
          <% end %>
        </li>
      </ul>
      <% end %>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>